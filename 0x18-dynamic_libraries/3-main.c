#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *s;
	s = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(s);
	return (0);	
}
